import { Box } from "../components";

const HomeScreen = () => {
	return (
		<Box
			color="white"
			width={100}
			height={30}
			background="aqua"
			justifyContent="center"
			alignItems="center"
			display="flex"
		>
			hello world
		</Box>
	);
};

export default HomeScreen;
